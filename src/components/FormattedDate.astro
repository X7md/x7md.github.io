---
export interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time class="text-sm" datetime={date.toISOString()}>
	{
		date.toLocaleDateString('ar-SA-u-ca-islamic-umalqura', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
